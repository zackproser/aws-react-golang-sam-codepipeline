version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file template.yaml --s3-bucket pageripper-serverless --output-template-file packaged.yaml
artifacts:
  files:
    - packaged.yaml
